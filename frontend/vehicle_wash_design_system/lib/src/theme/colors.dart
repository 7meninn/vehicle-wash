import 'package:flutter/material.dart';

class VerdantColors {
  // Brand colors from Design YAML
  static const Color primary = Color(0xFF767873);
  static const Color secondary = Color(0xFF767873);
  static const Color accent = Color(0xFF767873);

  // Background and surfaces
  static const Color background = Color(0xFF1C201C); // Obsidian
  static const Color surface = Color(0xFF151914); // Deep Forest
  static const Color surfaceElevated = Color(0xFF1B241A); // Deep Shadow
  
  // Specific naming from document
  static const Color warmSand = Color(0xFFF6F5F2);
  static const Color obsidian = Color(0xFF1C201C);
  
  // Text
  static const Color textPrimary = Color(0xFFF6F5F2);
  static const Color textSecondary = Color(0xFF767873);
  static const Color textInverse = Color(0xFF1C201C); // Dark text for light elements

  // Borders and dividers
  static const Color border = Color(0xFF27272A);

  // Transparencies
  static const Color whiteTransparent = Color(0x0CFFFFFF); // ~5% white
  static const Color blackTransparent = Color(0x0C000000); // ~5% black
}
